<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--인터넷 권한 선언-->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--카메라 권한 선언-->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <!--위치 권한 선언-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PpoPpiHouse">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PpoPpiHouse">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.PpoPpiHouse" />
        <activity
            android:name=".ui.onboarding.list.OnboardingListActivity"
            android:exported="true"
            android:theme="@style/Theme.PpoPpiHouse" />
        <activity
            android:name=".ui.onboarding.register.OnboardingRegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.PpoPpiHouse" />
        <activity
            android:name=".ui.diagnosis.select.SelectActivity"
            android:exported="false"
            android:theme="@style/Theme.PpoPpiHouse" />
        <activity
            android:name=".ui.diagnosis.camera.CameraActivity"
            android:exported="false"
            android:theme="@style/Theme.PpoPpiHouse" />
        <activity
            android:name=".ui.diagnosis.check.ChecklistActivity"
            android:exported="false"
            android:theme="@style/Theme.PpoPpiHouse" />
        <activity
            android:name=".ui.diagnosis.result.ResultActivity"
            android:exported="false"
            android:theme="@style/Theme.PpoPpiHouse" />
        <activity
            android:name=".ui.main.setting.edit.PetInfoActivity"
            android:exported="false"
            android:theme="@style/Theme.PpoPpiHouse" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
